# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anonymous/public key (safe to expose in client-side code)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Supabase service role key (NEVER expose this in client-side code!)
# Only use this for server-side operations or local development scripts
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Flutterwave Configuration
# Get these from your Flutterwave dashboard: https://dashboard.flutterwave.com/settings/apis

# Flutterwave Public Key (safe to expose in client-side code)
VITE_FLUTTERWAVE_PUBLIC_KEY=your-flutterwave-public-key

# Flutterwave Secret Key (NEVER expose this in client-side code!)
# Only use for server-side operations
FLUTTERWAVE_SECRET_KEY=your-flutterwave-secret-key

# Flutterwave Encryption Key
FLUTTERWAVE_ENCRYPTION_KEY=your-flutterwave-encryption-key

# Flutterwave Webhook Secret Hash (for verifying webhooks)
FLUTTERWAVE_WEBHOOK_SECRET=your-webhook-secret-hash
